{
  "name": "@helpdesk/server",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "seed:user": "bun --env-file .env run scripts/seed-user.ts"
  },
  "dependencies": {
    "@prisma/client": "5",
    "better-auth": "^1.6.22",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "prisma": "5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "typescript": "^5.5.3"
  }
}
